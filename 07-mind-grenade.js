const num1 = 5;
const num2 = 10;

function addValues() {
    console.log(`the sum is : ${num1 + num2}`);
}

// invoke the function
// when other file require this file
// ==>> this function will be executed
addValues();
