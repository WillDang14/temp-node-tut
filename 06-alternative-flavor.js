// Declare and export same time
module.exports.items = ["item1", "item2"];

// Declare variable then export it with another name
const person = {
    name: "bob",
};

module.exports.singlePerson = person;

/* ===================================== */
// console.log(module);
